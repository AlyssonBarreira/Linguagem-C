#include <stdio.h>

int main()
{
    int numero; // Declarando a variável

    numero = 40;  // Atribuindo um valor a variável

    printf("O valor da variavel numero: %d \n", numero);

    numero = 40 + 60;  // Atribuindo um novo valor a variavel

    printf("O novo valor da variavel numero: %d \n", numero);


    return 0;
}